interface:
  display_name: "MaxVideoAI Generation"
  short_description: "Quote, approve, generate, present, and recover AI media"
  default_prompt: "Use $generate to validate and quote my selected video request, then wait for my approval."
dependencies:
  tools:
    - type: "mcp"
      value: "maxvideoai"
      description: "MaxVideoAI account, reference, quote, generation, and recovery tools"
      transport: "streamable_http"
      url: "https://api.maxvideoai.com/mcp"
policy:
  allow_implicit_invocation: true
