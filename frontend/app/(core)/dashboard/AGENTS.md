# Dashboard Route Guide

The legacy dashboard is retired. `/dashboard` exists only as a compatibility redirect to `/app` so saved links do not break.

Do not add navigation back to this route or rebuild product UI here. New workspace entry behavior belongs in the authenticated `/app` routes.
