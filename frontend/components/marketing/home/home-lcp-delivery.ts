import posters from '@/config/home-posters.generated.json';

// Original source identity remains in home-lcp-image.ts. These generated URLs
// are display-only, content-addressed copies with durable browser caching.
export const HOME_LCP_DESKTOP_DELIVERY_SRC = posters.desktop.url;
export const HOME_LCP_MOBILE_DELIVERY_SRC = posters.mobile.url;
