# SDD ledger — plan: docs/superpowers/plans/2026-09-08-studio-connected-editor.md

Spec: docs/superpowers/plans/2026-09-08-app-studio-parallel-roadmap.md at ef3393c0d.
Initial base: ab2cb9fbd. Plan: e9727c5c9. App composition merge: 3bb6188e0.

Preflight interface table is versioned in the plan (tasks 1/2, 1/3, 1/4, 2/3, 3/4, 4/5 and self-consistency for every task).

Ruling: Import Studio selectively into current main + qualified app — old editor tree predates current catalog/API — cost if wrong: identify omitted dependencies in TypeScript/contracts and import only those dependencies explicitly.

Task 1: complete at e101e2840; independent review Approved (spec+quality),83 focused tests passed. 413 tests Node22, qa:editor388 tests +TypeScript+lint pass (2 inherited warnings). Source worktrees unchanged, manifests record32 tracked+2 untracked dirty paths. App merge normalized two inherited trailing spaces only. Minor corrected: export guide maximum output512MiB, matching renderer. Deployment/export cross-flow qualification remains explicit Task5 limit.
Audits: editor_inventory, app_visual_audit, mcp_persistence_audit, read-only except this worktree's report paths.
Known risk: legacy autosave deletes unknown sequences; connected projects require an atomic revision-checked snapshot writer and protection from legacy writers.
No external DB, paid generation, deployment, push or source worktree writes performed.

Ruling: Standalone SFX remains an additive shared audio contract, MMAudio V2 text only,3–30sec,0.1 vendor cent/sec and existing quote policy; no more expensive silent fallback — preserves historical functionality without pretending cinematic video audio is standalone — cost if wrong: separate contract commit can be reviewed/reverted, no paid calls authorized.
Ruling: Exclude only four newly imported global-owner suites media-library-round4/round5/security-round3 and wallet-render-round3 — these require unrelated historic shared-owner rewrites — cost: their19 failures are explicitly unqualified; export cross-flow financial safety is not claimed.
Ruling: Toolbox owns tool identities, eligibility and standalone surfaces; Studio consumes additive toolId/version/typed IDs and keeps old projects unchanged — coordinator explicitly assigned this boundary — cost: adapter integration is verified before delivery, not an automatic global migration.

Browser baseline: real local visitor Studio on3032, anonymous auth fixture3033, no inherited credentials or DB. Desktop light/dark1440x900 captured; mobile390x844 reveals tiny offscreen graph and overflowing toolbar. Starter product JPG omission fixed in e101e2840. Three other missing historic demo paths remain for Task2/3.
Toolbox contract source faf137284 inspected and imported independently; tests2/2 pass. Parent owns scripts/studio-local-preview.mjs and helper/tests, separate from Task1 review.

Task2 starts from998a8e611 plus documentation correction. Task brief and action audit issued; parent owns browser qualification/performance setup, implementer owns local Studio visual/interaction code and focused tests. Toolbox contract is imported but no new tool execution implementation should be invented before its own canonical adapter is qualified.

Environment evidence: connected-montage-disposable-postgres.test.ts creates a new PostgreSQL17 cluster via existing test helper, asserts socket-only/no network, writes+reads one row, then stops/removes its unique temp directory. Initial long prefix exceeded macOS socket path capacity; shortened stpg prefix passes (1/1). Explicit command PATH=/opt/homebrew/opt/postgresql@17/bin:$PATH plus Node22; no DATABASE_URL read or inherited.
Performance fixture inherited missing aerial-road.mp4 and legal endpoints failed without DB. Root replaces only test media URL with existing local6s1920x1080 video and mocks unrelated legal API, retaining real readers/rendering. BEFORE snapshot copied from332bf4abf to /private/tmp/studio-before.iuVmBm, separate preview3034/anonymousauth3035. Runtime dependencies symlinked read-only; no source/environment files copied. No performance claim yet.
Coordination Audio: task01a07e47-84df-7543-996d-74d39d496c67 owns autonomous creation now; shared SFX652a32ef0 and exact identity/original/measured-duration contracts transmitted. Studio keeps integration and persistence; no autonomous audio montage expansion.

Task2 browser: initial keyboard creation RED confirmed; GREEN4/4 after fixes (creation once + explicit inspector close and focus, track menu without right-click, immediate mobile inspector, typed connect/disconnect/undo preserving sources). Parent found and required fixes: mobile Settings initially only enabled another Inspector launcher; selection strip overflow; map covering canvas; focus lost on disconnected row; Live development errors silently became mock outputs. Agent fixed those with tests; final callback quote guard/commit pending. Existing all-layer timeline preload remains unchanged and explicitly unqualified as a performance improvement.
Parent readiness helper and8 smoke/timeline assertions now anchor on Projects action rather than removed product heading: e9259e41d. Parent browser owns3032; selected tab project_fff3051e-c023-4c67-8e26-d80486af3f9b is a disposable local starter, currently prompt edge removed by manual test; HMR reset undo, reconnect later if keeping preview.
Task3 deep audit complete: .superpowers/studio-audits/studio-media-contract-audit.md. Coordinator accepts additive shared signature: preserve IDs, external canonical assetId/ref, exact job-output tuples, facts only measured, account-owned read-only resolver with ready/kind/deleted checks and no implicit ensure. External/public ID is not media-public access. Preserve upload latency; Toolbox owns separate LibraryImageThumbnail modal correction. No broader library projection rewrite authorized.

Task 2: fix round 1/5 (2 addressed, 0 open — Connections Canvas history after timeline focus; native Play/Listen focus; commits 603f45a4b..957c593b6). Scoped re-review Approved, no new Critical/Important. Additional Copy real clipboard + explicit refusal and full-slot connection management verified. 12/12 E2E,397/397 tests,TS0,lint0/2 inherited warnings. Root responsive matrix8/8 at8cf596d54.
Task 2: minor (deferred): CanvasSelectionActions copyFailed alert persists on reopening Actions; clear at menu open in final polish and verify overlap/focus.
Task 2: complete (commits e9259e41d..957c593b6, review clean apart from deferred minor).
Ruling: Reserve final visual polish for optional empty port density and project-page text — real controls now work but card height still undermines user-requested clarity — cost if wrong: scoped presentation commit is reversible; no capabilities/handles may be removed.

Task 3: starts at dcb8eaa4b. Expanded task-3-brief.md is the binding dispatch; original plan scope unchanged, concrete risky gaps from approved media contract added. Fresh Astra implementer owns local media/runtime and isolated additive shared commits. Root owns verification documents/tests infrastructure only.
Root auth infrastructure: dcb8eaa4b, local-only ES256/JWKS fixture, real installed Supabase SDK signature verification, SSR cookie refresh, and real MCP principal adapter with SDK injection.3/3 tests after RED missing module. Does NOT yet prove Studio route auth, full login, DB persistence or private storage. PostgreSQL test now verifies major17 and exact data/socket paths before writes: b9d392faa,1/1green. Read-only fixture review dispatched to environment auditor.
Root environment audit: .superpowers/studio-audits/studio-persistence-test-environment.md. Use separate controlled Next launcher and DB, never anonymous3032for persistence proof.3034/3035 already held by root frozen BEFORE preview, so choose3036/3037 or free ports; preserve3032and3026.
Coordination addition: Audio relay requests MCP audio discovery/generate/retrieve. Asked main to assign one Audio/shared-MCP owner; Studio does not invent parallel audio pricing/execution or import Audio9be014f4d before probe fix/review. Studio consumes qualified usage routes/results v3 later. Main decision pending, independent media/montage work continues.

Coordination confirms Audio owns MCP Audio, Studio owns persisted video montage and project adapters only. Main Audio now includes corrected49118b66f and pricing policy user-approved×3 rounded up to0.05USD; Studio must later consume canonical helper/header, not compute its own margin. Source commit to consume pending Audio final report.
Task3 shared deltas qualified by implementer:4c986b84e (44 focused upload tests, additive refs/facts no additional probe) andcf525e7df (account-bound project handoff transport only, receiver not qualified). Sent4c986b84e to main/Audio/Toolbox. Task3 implementation remains uncommitted/unqualified; detailed checkpoint in task-3-report.md.
Task3 PAUSED atcf525e7df by root to isolate new composition findings. Agent studio_media_implementation stopped its own targeted runners, preserves all edits and awaits followup; preview3032 untouched.
Task2 composition fix: main independently confirmed Chat ignoresMock and invokes provider without quote; renderer1440p gives1080p; export IDs collapse punctuation/truncate and collide across owners. Original implementer studio_interface_implementation resumed with task-2-integration-fix-brief.md at BASEcf525e7df. Product writes serialized; Task3 still paused. Only overlappingstudio-copy.ts has six pendingprojectMedia labels which must stay outside fix commit.
Ruling: Chat Live stays unavailable at callback and route until a canonical quote/authorization contract exists; Mock produces explicitly simulated local content with no fetch/provider — main explicitly approved because no shared Chat billing owner exists — cost: Live Chat is a disclosed import limitation, not a secretly paid feature. Multimedia generation remains on its existing quote owners.
Root5303c6f8b proves real Studio routes through signed local Auth and PostgreSQL17 in a committed Git snapshot on its own free loopback port.4/4 route+SDKtests green, teardown removes only unique owned snapshot/cluster after stopping its child. No existing3032/3034server reuse. This still does not qualify montage persistence or browser/privateS3.
Read-only playback audit complete: .superpowers/studio-audits/studio-playback-window-audit.md. Final polish candidate window active+transition+next1.5sec, retaining legacy prep; global linked audio ownership must not be windowed, detached refs must pause/clear. Existing firstPlay/scrub measures onlyclock; actual frame/audio evidence required before claiming performance safety.

Composition fix eb791a266 completed by original implementer: MockChat local, Live callback/route failclosed,1440p actual dimensions, export identity digest(owner,exactkey) with historic lookup.93targetedtests+1E2E+TS pass; full418 includes6Task3pendingfailures, lint4warnings includes2Task3. Independent scoped review by studio_baseline_review in progress; Task3 stays paused until gate. Root separately verified actual cookie+Bearer Chat503/anonymous401 in owned Git snapshot/localPG runtime,1/1 route suite at cb4a0bf8e.
Root cb4a0bf8e adds2synthetic6sH264/AAC moving fixtures with measured facts/hashes,230784bytes total. No user/provider media. Expanded Task4brief binds strict new-project-only command, canonical asset IDs, exact-key atomicreceipt, CAS+alllegacywriterguards, privateoriginal renewal, true UI/MCP paths and local real-browser decoding proof.
Coordination update: main now owns remaining Billing/Toolbox/MCPAudio; old tasks archived and their worktrees removed by app, Git SHAs remain. Use Git refs only for future imports, coordinate interfaces with main exclusively. User explicitly permits more Astra subagents; available seats reused for bounded read-only UX/review and targetedMCPpreflight while one product implementer writes. No new peripheral work.
Clarity audit finished .superpowers/studio-audits/studio-final-clarity-audit.md: pure visible required/connected projection, retain allReactFlowhandles with nonzero anchorgeometry and internalsupdate, optionalemptyviaConnections with capacity/focusfallback, Projectsonephrase, Copyselection-scopedlatepromiseguard. Finalpolish only after media/montage qualification.
Composition eb791a266 independent review Approved, noCritical/Important. Task3 resumed withmediaimplementation onlywriter;9d7bb92d4 separate shared export of existing URLvalidator sentmain. Root QA51e286901 nativeframes/audio3unit+1E2E passed; review foundcachedframecouldfalselyprovepostseek, corrected e1c1e1848 requiresnewcallback+currentTime+visibility andactiveAformute; rerun1/1 andscopedreviewApproved. Actualtrace4frames,1.133333s video,1.162943s audio,RMS0.071739,Bseek1s. Docs d416b9e2b updated, noS3/DB/persistenceclaimfromthisfixture.
Root freshvisualprepolish captures showfitcanvasstillunderfloatingHUDandcardtoolong; Task5brief expandedboundedclaritypatchfitusablearea+optionalports+Projects+Copy. No automaticexpansiontowindowedplaybackengine:150readersdebt mayremaindocumentedifnoregessionandnotrequiredminimalmontage. Userwantscompletedlot, prioritizefunctionalmedia+persistedmontage+clarityoverperipherals.

Task3 frozen at bd7aeb3cb, measured hasAudio shared interface00c0cfa3b, final report28475b388.429focused tests +45upload +11E2E pass, TS/exposure/diff0, lint0errors/2inherited warnings. Independent spec+quality review dispatched to studio_baseline_review using five immutable Task3-only packages. Other product writers stopped. Root separately exercises the actual media resolver HTTP through signed cookie/Bearer and freshly verified localPG: exact refs/original/facts, owner and hidden-job refusals,1/1 pass. No private byte-delivery claim.
Agent capacity ruling: user authorizes more Astra agents, but tool limits this team to root+3 persistent seats; an earlier fresh spawn was refused. Reuse an idle seat with a new bounded task instead of creating an unrelated user task or writing source worktrees. studio_interface_implementation performs read-only Task4 writer/CAS mapping while Task3 is reviewed; implementation still waits for its gate. This is the capacity fallback to the fresh-implementer preference, not permission for concurrent product edits.

Task3 Approved (no Critical/Important), Minor cache upload canvas fixed f426be90c and scoped re-review Approved;5/5 DOM tests. All Astra agents completed and stopped writes. Main relays explicit user instruction to finish with Sol high and use Sol high for new delegation. Fresh spawn now succeeds after all previous agents completed: studio_persisted_montage_sol receives full Task4 brief, no child delegation, sole product writer. Earlier capacity observation was transient, not a permanent inability to create a fresh implementer. Preserve all prior passations.
Root QA7cbafa1db independent review Approved. Real MCP foundation HTTP fixture now passes1/1: signed Bearer, invalid/no token and cookie-only rejection, true SSE initialize/discovery, both montage tools absent by default and two persisted audit events on verified local PG. NextURL normalizes loopback to localhost, so test HTTP connects exact owned IPv4 child with explicit localhost Host; no global middleware modification. This does not qualify montage transaction or full acquisition funnel. Root owns runtime/Auth/HTTP/byte fixtures and verification docs; Sol owns Task4 product/tests except those reserved paths.

Task4 server baseline20a181c35 includes25writer paths plus4rootQA paths because shared Git index staging overlapped; no content lost or source changed.87919e9d7 only removes2EOFblanklines. Full29path provenance in docs/engineering/studio-task4-integration-review.md. All writers now use git commit --only with explicit paths and index checks. Main coordinator approved preserving history, not rewriting it.
Task4 shared MCP1c41f3d16 strictly isolated, studioMontageCreation:false. Root first realHTTP1/1GREEN then strengthened businesserror assertion RED; cdb716027 fixes expected error mapping. Root findings require atomic project/sequences read and shared parent/source locks;1c9eafa92 adds those plus realPGbarriers/data_directory/cleanup. Root HTTP now2/2GREEN: actualMCPcreate, SQLorder/trims/owner/receipt, signedprivateaccess, atomicread, UIcookie/MCPexactreplayafterCAS, alllegacywritersrefused, no42=>503/retryable/noimplicitconnectedDDL. Client Task4 and final independent review still open.
Root private storage QA a940929ef reviewed Approved, realproductionGETsigner with fakecredentials/localbytes,7/7aggregate tests. Sol QA browserhelper3b5bfc06b reviewedrootApproved,1/1countertest: isolatedChromiumcontexts, exactlocalallowlist, blockedserviceworkers, noexternalHTTP, signedRange/unsignedrefusal, noqueriesinjournal. Root realbrowser acceptance34d5cc048 RED onprivate readerreadyState0 beforeclientaccesswiring; projectopensViewer2clips/order/trims, noJSerror, expectedlegacyautosave409, screenshotinspected. Later browserassertions notyetqualified.
Additional freshSolreviewseat spawn refused by current agent limit; reuse existingSolQA after its fixture completion for read-only immutable server/MCP review. NoAstra reactivation or unrelated sidebar task. Sole productwriter continuesclient, rootownsHTTP/browser anddocs. Task5clarity24matrix remainsintentionalRED untilpolish, no performancegainclaim.

Task4 independent server review: 3 Important, no Critical (invalid snapshot structure, durable original frame provenance, opaque unexpected server failures). Root reproduced malformed timeline [null] returning200 instead of400. Writer correction7cf6a5b57 now passes root enhanced realHTTP2/2 in22.91s, including local SQL failure rollback, exact MCP business errors and immutable request_payload/montageSource after CAS/replay. Independent server re-review dispatched read-only; client remains in progress. Root browser test strengthened following QA review: renewal requires a successful real media-access POST and a different signed URL for the same object, plus preserved playhead; ownerB fresh-context refusal, local draft conflict/reload and no signed tokens in browser storage. Those browser assertions are not yet qualified.

Task4 server corrective re-review Approved: de02e5825 closes live-sequence reference renewal after bin removal and exact legacy409 preservation; root HTTP3/3 green. MCP shared chain1c41f3d16→cdb716027→c774f6a6e restores strict ten-flag readers/support while gatefalse; root81/81 serial MCP suites green. Minor historical diagnostic counts remain, no runtime activation. Client6fd412782 plus1e56fd2a5,704c9d974,909e37f60,4a72b3d04,139bf2e01 now passes real browser1/1 in36.31s, immutable snapshotdb6560345: native private frames/AAC, renewal with new valid signature, seek, no hydration PUT, first edit SQLrev1, stale409/localdraft, trueReload, fresh reopen, ownerB404/no media. Earlier failures traced separately to root instrumentation, hydration timestamp/fingerprint, and passive-toast pointer events; fixed without weakening assertions. Client independent review in progress, UI creation builder still in progress. Task5 brief adds only-new-montage fit presentation and true media-Undo availability; legacy transform semantics stay unchanged.

Task4 client independent review Needs changes: four Important (live-reference discovery on fresh open; truthful save/exit and visible autosave failure; account/project local-draft recovery; immediate hydrated-state purge on in-place Auth identity change). Root reproduced retained clip with no video element after real CAS bin removal and fresh reopen on 7caa7cea2, RED 44.84s. Sole Sol product writer owns the correction; root extends real-browser assertions and docs. UI builder frozen separately at 89af69248, reported 43/43 and TypeScript green, independent Sol UI review underway. Root browser now separates retained-reference and mobile keyboard UI-creation sub-tests so one failure does not hide the other. Coordinator received exact server prerequisite ownership/chain, with explicit no-integration until client/final qualification.

Task4 UI review Needs changes on 89af69248: pending intent mutation, silent validation and fractional FPS trim overflow; contextual accessible names Minor. fa48e19b4 freezes intent; root real mobile UI sub-test GREEN 5.69s, including true server commit then lost response, exact key/payload retry, same project/one receipt and Studio reopening. Root corrected FPS selector by exact accessible combobox role, proven with isolated Chromium label semantics; no product label change. Root Auth peer-event counterexample now RED on fa48: actual SDK updates avatar/account B and shows owner denial, but prior clips/private frame remain rendered. The notification is controlled fixture input, not full login; consumer/routes are real. Client corrective writer remains active, root adds actual 409 draft recovery/manual exit and 503 offline recovery criteria before next run. Task5 gains mobile ordered-list nested-scroll polish within existing scope.

Task4 corrections frozen: bccd76266 (four client findings),77d05b569 (root-discovered stale base revision after recovery),fa48e19b4/adfe2f29c (UI intent freeze/timeout/validation/FPS/accessibility). Root reproduced bccd local draft revision incorrectly changing0→1, RED35.04s; corrected double reopen keeps dirty0/noPUT. Latest real-browser snapshotadfe2f29c GREEN5/5,61.36s, including UI fractional source-end trim, lost reply replay, stable Auth denial/purge/no-private-bytes, actual SQL bin deletion and exact private sourceB. QA root review requested these last causal strengthens plus local failed-open cleanup; now implemented. Product and QA corrective re-review pending; Task4 not yet Approved, Task5 not begun. No external writes or integration.

Task4 re-review closes QA source identity/Auth stability but finds three product corners: dispose can resolve pending Projects as ready without ACK; unresolved initial project exposes legacy save exit; library GET failure hidden by validation/no retry. Root actual browser proves pending-exit Auth switch navigates without ACK on adfe, RED58.63s; initial-read readiness test added. Writer corrects before gate, Task5 still waiting. Root build d139 complete Git snapshot at /private/tmp/studio-build.ZXlxH2: offline prebuild passed, webpack compile42s passed, lint failed prefer-const on new queue declaration;4a1d7499d fixes that line separately. No inherited DB/provider env; build retry/final build still pending. User requested extra agents; fresh Sol manifest delegation refused by thread limit, existing Sol seats reused, no Astra reactivation.

Intermediate full build 9deed7a2d in /private/tmp/studio-build.eLy8GT now exit0: prebuild, webpack43s, lint/types,867/867 pages, traces/optimization and sitemap postbuild. No DB/provider env. This is not the final post-polish build. Root initial GET-held exit is also causal RED on adfe,5.75s; full six-test run64.50s has two failing child scenarios plus parent. Independent QA d139+9deed Approved. Product fixes6475a3d35→b2ee9f500 frozen with writer13/13 client/UI and58/58 architecture green. Corrective Sol review and root six-test browser run ongoing, now including actual dark-theme DOM and visible library503/retry preserving title/settings in the same dialog. Task4 not yet complete; Task5 still gated.

Three prior Important closed, then root real retry exposed lost keyboard focus because Retry unmounted.827241cf4 local focus correction plus causal DOM test reviewed Approved; full root connected browser6/6 GREEN63.73s on that SHA (real dark DOM, retry intent/focus/Escape, all prior private persistence paths). Root legacy preservation check found new exitReady guard traps UI-created local drafts on API401/503, RED7.3/14.2s; source review confirms Projects cache loses persistenceMode. Root36176c871 owns new local-draft E2E, including real prompt edit/reopen and no server writer on local exit. Sole product writer corrects local/unresolved/connected distinction; Task4 remains in progress. All34 historic dirty-source hashes and HEAD rechecked unchanged.
