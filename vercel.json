{
  "installCommand": "npm install --workspace frontend --omit=dev",
  "buildCommand": "npm run vercel-build",
  "builds": [
    { "src": "frontend/package.json", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "frontend/$1" }
  ]
}
