{
  "installCommand": "cd frontend && npm install --omit=dev",
  "buildCommand": "NPM_CONFIG_WORKSPACES=false npm run vercel-build",
  "builds": [
    { "src": "frontend/package.json", "use": "@vercel/next" }
  ],
  "routes": [],
  "crons": [
    { "path": "/api/stripe/batch-transfers", "schedule": "0 3 * * *" }
  ]
}
