# Reprise de finition avec Sol / élevé — 8 septembre 2026

L’utilisateur demande explicitement Sol en raisonnement élevé **sur les deux tâches** : principale01a07920-6fc3-7131-911d-321ce840ffdd et Studio01a07e2a-7f4d-7c83-b24d-11b6008f00d9. Le message de poursuite Studio a été envoyé avec `gpt-5.6-sol` / `high`. Faire de même pour toute délégation ultérieure. Ne pas recréer de tâches sidebar archivées ; leurs passations restent du travail à finir ici. Branche isolée `codex/app-catalogue-validation`, aucun merge/push/deploy demandé.

## État à reprendre

- Billing lifecycle c453e604b + rapport5ecfaecec : revue indépendante PASS. Lectures Billing9a4a42af0 : revue indépendante PASS, vrais GET/PG et comparaison contrôlée locale. Voir consolidation et rapports SDD.
- Toolbox9352538a7/54a0770c1 puis correctifs78cc5c59b/b91b44942 : revue indépendante PASS.26 tests, vrai cron/PG, observations réellement annulables, terminal completion/refund sérialisé, résolution Restore contrôlée. Registre qualifications vide, donc candidats toujours inactifs. Ne pas confondre intégrité du code et qualité fournisseur commerciale.
- MCP Audio Task1 : implémentation racine figée dans le même commit que cette note. Réutilisation sélective du WIP e274b0871 ; requête Audio stricte/versionnée, références ToolAssetRef, capacités réelles, codecs quotes avec filtre surface SQL avant mutation, lecteur union top-up/activité, réservation/exécution web partagée.113 tests couvrants, zéro skip/échec ; lint ciblé et TypeScript frontend finaux passent. Revue indépendante de ce seul commit requise avant Task2. Rapports `.superpowers/sdd/2026-09-08-mcp-audio-completion/task-1-report.md` et guide `docs/engineering/mcp-audio.md`.
- La preuve du runner Audio a trouvé un défaut réel : décimales et Song null incompatibles avec `app_jobs.duration_sec INTEGER NOT NULL`. Correction : zéro inconnu au départ, entier supérieur SQL, mesure exacte dans settings_snapshot.measuredDurationSec/mediaFacts et réponse.123,4s reste exact ; SQL124. Pas de migration globale. Projection exacte dans job_outputs à terminer au lot résultats.
- Agent interne `active_draft_completion` était en fin d’implémentation (Astra), invité à figer et écrire sa passation pour Sol. Ses fichiers workspace/hooks/contrats restent **non committés**, distincts du lot Audio. Ne pas écraser ni déclarer accepté : lire son rapport dans `.superpowers/sdd/2026-09-08-workspace-active-draft-continuity/`. Dernier point : fichiers fonctionnels, cas logout legacy/stale upload/mirror/requested job couverts ; fin de vérification/proofs/docs en cours. Demander sa passation avant de l’arrêter s’il n’a pas déjà terminé. Les autres agents de relecture sont terminés.

## Travail restant, dans l’ordre des dépendances

1. Terminer et revoir le brouillon actif selon `2026-09-08-workspace-active-draft-continuity.md`. Vraie QA navigation Vidéo→Image/Audio→Vidéo, refs existantes, refresh et récupération. Pas de génération/import payant.
2. Revue indépendante MCP Audio Task1, puis Task2 et Task3 du plan `2026-09-08-mcp-audio-completion.md` : préparation/confirmation OAuth exacte, sources possédées et identité revalidées, prix/limites cumulatives, wallet/job/claim atomiques, retries sans nouvelle soumission, échecs/remboursements, outils publics, vrais états/résultats/audio player et nouvel App v5 immutable. Aucun outil public Audio generation n’est encore enregistré. Le runtime reste synchrone, pas de reprise automatique après crash.
3. Retour vers un emplacement de référence : contract media-handoff par compte/token ; legacy Image stocke encore globalement et son debounce doit être qualifié/annulé avant handoff privé. Ce travail n’est pas résolu par la seule sauvegarde active Vidéo.
4. Studio reste dans sa tâche dédiée. Son Task3 média est approuvé à bd7aeb3cb (rapport28475b388), retouche cache upload mineure avant relais ; test vrai résolveur HTTP cookie/Bearer+PG7cbafa1db approuvé. Task4 timeline persistée/MCP vidéo en cours. Demander sa chaîne exacte acceptée après finition, puis intégrer prudemment les owners/deps/interfaces/UI (gros import), sans merger sa composition historique.
5. Interfaces Studio non encore importées :00c0cfa3b garde le hasAudio déjà sondé dans upload/facts (aucun nouveau probe) ;9d7bb92d4 exporte seulement validReferenceMediaUrl. Déjà intégrés et à SKIP :8a74ab753→1830bf652,4c986b84e→f55401ad7,746a0cf49→9c9bc9ea4. P0 Toolbox/SFX déjà importés aussi. Base Studio très large, voir sa chaîne documentaire plutôt qu’un cherry-pick global. Réserves Studio antérieures corrigées eb791a266 et test auth07b1e3c42. Export1440p legacy multiplier1 vs1080p1,5/2160p3 : pas de prix inventé ni export réel avant politique clarifiée.
6. Passe visuelle finale : Audio CTA320/390 quand tarif indisponible/retry, popover wallet qui reste au focus après navigation, UUID de reçu trop présent dans ligne repliée (le garder dans détails), tous menus/références/media/tools/activity et thèmes clair/sombre EN/FR. Puis suite composée/types/lints/exposure/build et vrais parcours.

## Environnement et QA

Preview gardée3026 via `.superpowers/artifacts/2026-09-07-app-catalogue-validation/start-connected.cjs`, PTY32272 (vérifier état). Base clone isolée `br-blue-dream-ae1mk5p7`, projetshy-flower-71253790, expiry10septembre16:43:50UTC ; baseproductionbr-late-term-aeo22xpz non expirante. Clés provider/paiement/écriture stockage retirées par launcher ;503 devisAudio attendus. Aucun accès prod pour tests.

Validation : `node .superpowers/artifacts/2026-09-07-connected-app-review/run-validation.cjs <commande>` ; PostgreSQL jetable explicite, concurrence≤4. Pas de build dans .next pendant dev, arrêter/repartir proprement ou build isolé.

Browser autorisé via skill Browser et Node REPL browser-client seulement. Session persistante : `browser`, `toolboxReviewTab` (iab25), `toolboxReviewViewport`. User tab26 /app/audio à laisser. Root QA25 actuellement thème sombre,1440×1000 ; vient de cliquer Create depuis Billing, snapshot juste après affichait encore Billing pendant navigation/HMR. Inspecter après stabilisation, ne pas conclure défaut de route sur un snapshot immédiat. Restaurer thème clair et viewport à la fin. La longue génération types/backend provoque des recompilations ; ne pas prétendre une mesure CWV sur cet environnement en mouvement.
